#include <bits/stdc++.h>

using namespace std;


int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    for (int i = 0; i < 100; i++) {
        string s;
        getline(cin, s);
        cout << s << '\n';
    }
}