//
// PS on 24. 5. 5, 오전 1:11.
//
#include <bits/stdc++.h>

using namespace std;

double a, b;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> a >> b;

    cout.precision(12);
    cout << fixed;
    cout << a / b;
}